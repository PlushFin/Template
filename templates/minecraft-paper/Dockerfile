FROM itzg/minecraft-server:stable-java21

# Optional defaults baked into image.
ENV TYPE=PAPER \
    EULA=TRUE \
    ENABLE_ROLLING_LOGS=true

# Data directory expected by platform storage attach.
VOLUME ["/data"]

